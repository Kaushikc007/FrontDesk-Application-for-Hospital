# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_mysql_password
DB_NAME=clinic_management
DB_SYNC=true
DB_LOGGING=false

# JWT Configuration  
JWT_SECRET=your_super_secret_jwt_key_here_at_least_32_characters_long
JWT_EXPIRES_IN=24h

# MongoDB (can be removed after migration)
MONGODB_URI=mongodb://localhost:27017/clinic-management
